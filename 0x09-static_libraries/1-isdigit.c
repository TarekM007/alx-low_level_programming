#include "main.h"
/**
 * _isdigit - checks for a digit
 *
 * @c: checks the input of the function
 *
 * Return: returns 1 if 'c' is digit otherwise always 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	return (1);
return (0);
}
