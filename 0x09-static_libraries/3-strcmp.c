#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 *
 * @s1 : a pointer to an char as parameter
 *
 * @s2 : a pointer to an char as parameter
 *
 * Return: returns value
 */
int _strcmp(char *s1, char *s2)
{
int ret;

ret = strcmp(s1, s2);
return (ret);
}
