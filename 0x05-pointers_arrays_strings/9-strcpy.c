#include <string.h>
#include "main.h"
/**
 * _strcpy -   copies strings
 *
 * @dest : a pointer to a char as parameter
 *
 * @src : a pointer to a char as parameter
 *
 * Return: returns value
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
