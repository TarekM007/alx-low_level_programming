#include <string.h>
#include "main.h"
/**
 * _strlen -  length of a string
 *
 * @s : a pointer to an int as parameter
 *
 * Return: returns value
 */
int _strlen(char *s)
{
int l = strlen(s);
return (l);
}
