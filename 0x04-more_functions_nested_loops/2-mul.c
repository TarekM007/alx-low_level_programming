#include "main.h"
/**
 * mul - checks if the character is uppercase or not
 *
 * @a: checks the input of the function
 *
 * @b: checks the input of the function
 * Return: returns 1 if 'c' is uppercase otherwise always 0
 */
int mul(int a, int b)
{
int res = a * b;

return (res);
}
